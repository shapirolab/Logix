Make a membrane:
---------------

/* A(x) ::= membrane<<B>>. */

  "A"("_var"(x), Ambient) :-
    known(x) :
      write_channel(membrane([{"_var"(x), "_var"("A.n_x")}], Reply), Ambient) |
	"A.membrane"+("_var"(x)="_var"(A.n_x)).

  "A.n"("_var"(x), Reply, Ambient) :-
    Reply =?= 0 |
	"B".

/* A(x) ::= enter x, B.

  "A"("_var"(x), Ambient) :-
    known(x) :
      write_channel(send
