The .cp modules in this directory may be compiled and executed - e.g.

    c(eg1)
    run(eg1,20)

The sub-directories vesicle and complex may be compiled and executed - e.g.

    c(complex)
    run(complex,10)
